# Step 04 Report — red-catch2-red-tests

**프로젝트:** Feedback Analyzer C++17  
**갱신일:** 2026-05-22  
**완료 단계:** 4-A — Catch2 RED 테스트 (P0 1차 착수)  
**브랜치:** `red`  
**TC-ID:** **FA-TC-01 ~ FA-TC-15** (Type C) · **FA-TC-16, 17, 29** (Type A · DEF RED 게이트)

---

## 1. 요약

`spec` 완료 후 `red` 브랜치에서 Catch2 FetchContent·`feedback_analyzer_tests` 타겟을 도입하고, 레거시 `src/cpp/` **무수정** 원칙 하에 Legacy Adapter·Fixture로 P0 TC를 RED 구현했다. FA-TC-01~15는 레거시 관측(Type C) **PASS**, DEF 계약 TC 3건은 목표 AC 기준 **의도적 FAIL**이다.

---

## 2. CoT 요약

| TC-ID | Given | When | Then (목표 AC) | RED 실패 이유 | GREEN 최소 가설 |
|-------|-------|------|----------------|---------------|-----------------|
| **FA-TC-01~07** | 단일/빈 피드백 | `LegacyAdapter::sent` | 긍·중·부 건수 고정 | — (PASS) | DomainSentimentAnalyzer 동일 규칙 |
| **FA-TC-09~15** | 카테고리 문장 | `LegacyAdapter::kw` | 5카테고리 집계 | — (PASS) | DomainKeywordCounter |
| **FA-TC-06** | `def01NeutralAmbiguous` | sent | 중립=1 | — (집계측 DEF-01 관측) | fil과 규칙 통일 |
| **FA-TC-16** | `mainOnlyQuality` (`품질`) | kw + fil(품질) | kw≥1 **and** fil≥1 | `0 >= 1` (main 스킵) | fil에 main 스캔 추가 |
| **FA-TC-17** | def01 | fil(중립) | 1건 | `0 == 1` | S_KEYWORDS ↔ Constants 통일 |
| **FA-TC-29** | def01 | sent 중립=1 + fil(중립) | 1건 일치 | `0 == 1` | AC-SENT-01 Domain 필터 |

> **FA-TC-16:** Test Plan의 `"배송"` main-only는 레거시 `status` 서브키에 `"배송"`이 있어 fil이 통과할 수 있음 → DEF-02 RED 재현에 **`품질` main-only** 사용.

---

## 3. 변경 파일

| 경로 | 변경 유형 | 설명 |
|------|-----------|------|
| `CMakeLists.txt` | 수정 | Catch2 v3.5.4 FetchContent, `feedback_analyzer_tests`, `catch_discover_tests` |
| `tests/test_main.cpp` | 신규 | Catch2 Session + `Constants`/`Filters` init |
| `tests/test_env.hpp` | 신규 | 테스트 환경 초기화 |
| `tests/test_text_analyzer_sent.cpp` | 신규 | FA-TC-01 ~ 07 |
| `tests/test_text_analyzer_kw.cpp` | 신규 | FA-TC-09 ~ 15 (`\uXXXX` UTF-8 안정화) |
| `tests/test_consistency.cpp` | 신규 | FA-TC-16, 17, 29 (DEF RED) |
| `tests/fixtures/FeedbackFixtures.h` | 신규 | empty, def01, mainOnly*, overlap |
| `tests/fixtures/Expectations.h` | 신규 | `expectSentiment`, `expectKeywordGe` |
| `tests/support/LegacyTextAnalyzerAdapter.hpp` | 신규 | `TextAnalyzer` 위임 |
| `tests/support/LegacyFiltersAdapter.hpp` | 신규 | `Filters::fil` + stdout 격리 |
| `Report/04.red-catch2-red-tests-report-2026-05-22.md` | 신규 | 본 Report |
| `Prompt/04.red-catch2-red-tests-transcript-2026-05-22_prompt.md` | 신규 | 세션 Transcript |
| `Prompt/full-transcript-refactor-first-2026-05-22_prompt.md` | 수정 | Step 04 누적 갱신 |

**미포함 (의도적):** `build/`, `src/cpp/**` (레거시 diff 0), `prompt초안.md`, `prompt1.md`

**red 커밋 수:** 20건 (`test(red):` 17 + `fix(red):` 2 + 본 문서 커밋 예정)

---

## 4. 빌드 · 테스트 결과

| 명령 | 결과 |
|------|------|
| `cmake -S . -B build` | ✅ Catch2 FetchContent 구성 완료 |
| `cmake --build build --target feedback_analyzer_tests` | ✅ `[100%] Built target feedback_analyzer_tests` |
| `cmake --build build --target feedback_analyzer` | ✅ 레거시 실행 파일 유지 |
| `ctest --test-dir build` | **17 tests** — **14 PASS / 3 FAIL** (의도적 RED) |

**의도적 FAIL (DEF 게이트):**

| # | TEST_CASE | 메시지 |
|---|-----------|--------|
| 1 | `FA_TC_16_KeywordMainOnlyMismatch` | `REQUIRE(filtered.size() >= 1)` → `0 >= 1` |
| 2 | `FA_TC_17_FilterNeutralZero` | `REQUIRE(filtered.size() == 1)` → `0 == 1` |
| 3 | `FA_TC_29_ConsistencySentFilNeutral` | 동일 |

**PASS (14):** FA-TC-01~07, 09~15

---

## 5. 산출물 경로

- 테스트 소스: `tests/test_text_analyzer_sent.cpp`, `tests/test_text_analyzer_kw.cpp`, `tests/test_consistency.cpp`
- Seam/Fixture: `tests/support/`, `tests/fixtures/`
- 실행 파일: `build/feedback_analyzer_tests.exe`
- Test Plan: [`docs/test_plan.md`](../docs/test_plan.md)
- Report: `Report/04.red-catch2-red-tests-report-2026-05-22.md`
- Prompt: `Prompt/04.red-catch2-red-tests-transcript-2026-05-22_prompt.md`

---

## 6. 다음 Step / 브랜치

| 항목 | 내용 |
|------|------|
| **현재 브랜치** | `red` (RED 1차 착수 — FA-TC-01~15·DEF 3건 완료) |
| **다음 Step** | **4-A 계속** — FA-TC-18 ~ FA-TC-30 (`test_filters.cpp`, `test_consistency.cpp` 확장) |
| **이후 브랜치** | `green` — `tests/support/Domain*` + FA-TC Green · 커버리지 게이트 · GM |
| **제약** | `src/cpp/` 레거시 수정 금지 (green/refactoring까지) |
| **참조** | `@docs/test_plan.md` §2.2 RED 1차 착수 · §부록 B |

---

## 7. README TODO 동기화

| # | 단계 | 진행 |
|---|------|------|
| 4-A | red — FA-TC RED | **진행 중** (01~15 + 16·17·29) |
| 4-B | green — FA-TC PASS | 대기 |
| 4-C | green — 커버리지 90/85 | 대기 |
| 8 | green — Golden Master | 대기 |

---

*문서·배포 Step 04 — 2026-05-22*
